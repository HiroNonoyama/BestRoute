import React from "react";

import styles from "../../styles/containers/Map.scss";

const Map = () => (
  <div className={styles.container}>
    <div className={styles.map} id="map" />
  </div>
);

export default Map;
